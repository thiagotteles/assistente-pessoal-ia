# Template: Entrada Categorizada

## Uso
Template para criar arquivos categorizados automaticamente pelo Agente Organizador após processamento do daily-dump.md.

## Template YAML
```yaml
---
# Metadados automáticos
id: "{{ uuid }}"
type: "{{ contexto|decisao|projeto|pessoa }}"
created: "{{ iso_timestamp }}"
updated: "{{ iso_timestamp }}"
source_agent: "organizador"

# Classificação
category: "{{ categoria_detectada }}"
subcategory: "{{ subcategoria_especifica }}"
priority: "{{ alta|media|baixa }}"

# Contexto TDAH
emotional_indicators:
  - "{{ stress|overwhelm|procrastination|normal }}"
urgency_calibration:
  perceived: "{{ urgency_level_original }}"
  actual: "{{ urgency_level_calibrated }}"

# Relacionamentos automáticos
related_people:
  - "[[{{ pessoa_detectada }}]]"
related_projects:
  - "[[{{ projeto_detectado }}]]"

# Tags inteligentes
tags:
  - "{{ tipo_conteudo }}"
  - "{{ contexto_emocional }}"
  - "{{ projeto_tag }}"
  - "tdah"  # sempre presente

# Processamento
processing:
  questions_asked: {{ numero_perguntas }}
  user_responses:
    - question: "{{ pergunta }}"
      answer: "{{ resposta }}"
  confidence_score: {{ 0.0_to_1.0 }}

# Próximas ações detectadas
extracted_todos:
  - action: "{{ acao_detectada }}"
    priority: "{{ prioridade }}"
    deadline: "{{ prazo_se_detectado }}"

# Aprendizado contínuo
patterns_learned:
  - "{{ padrao_identificado }}"

# Cross-referencing
similar_entries:
  - "[[{{ entrada_similar }}]]"
---

# {{ titulo_gerado_automaticamente }}

{{ conteudo_original_com_links_automaticos }}

## Contexto Adicional

### Análise do Organizador
- **Prioridade detectada**: {{ analise_prioridade }}
- **Estado emocional**: {{ analise_emocional }}
- **Próximas ações sugeridas**: {{ acoes_sugeridas }}

### Insights Pessoais
{{ insights_baseados_na_memoria_do_usuario }}

### Padrões Identificados
{{ padroes_detectados_para_aprendizado }}

---
*Entrada processada automaticamente pelo Agente Organizador Sofia*
*Processado em: {{ timestamp_processamento }}*